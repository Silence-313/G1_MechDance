# 服务器与机器人连接指南

## 整体流程

```
本机 ──SSH──▶ 服务器 ──网线直连──▶ 机器人
      (WiFi)          (enp6s0)
```

1. 本机通过 WiFi SSH 连接到服务器（10.101.x.x）
2. 服务器通过网线与机器人直连（192.168.123.99）
3. 本机 scp 上传可执行文件 → 服务器运行 → 通过 DDS 控制机器人

---

## 一、连接服务器

**前提：** 服务器已通电并连接显示器，本机与服务器在同一网络下。

| 步骤 | 操作 | 命令 |
|:--:|------|------|
| 1 | 启动电源，等待 Ubuntu OS 加载完成 | — |
| 2 | 在服务器显示屏上按 **Enter**，输入用户名和密码 | — |
| 3 | 登录后查看 IP 地址 | `ip a` |
| 4 | 查找 **10.101 开头**的 IP，从本机 SSH 连接 | `ssh ubuntu@<serverip>` |

> **步骤 3 参考：** 10.101 段 IP 通常出现在无线网卡上。
>
> ```
> ubuntu@ubuntu-serve:~$ ip a
> 1: lo:          inet 127.0.0.1          # 回环
> 2: enp6s0:      inet 192.168.123.99     # 有线 → 机器人
> 3: wlx...:      inet 10.101.194.227/22  # 无线 → 本机 SSH 用这个 ✓
> 4: docker0:     inet 172.17.0.1         # Docker
> ...
> ```

**实际 IP 可能变化，每次连接前确认。**

---

## 二、连接机器人

**前提：** 机器人已开机，与服务器通过网线直连（接口 `enp6s0`）。

| 步骤 | 操作 | 命令 |
|:--:|------|------|
| 1 | 在服务器上 ping 机器人 | `ping 192.168.123.99` |

- ✅ **有响应** → 连接成功，跳到第三章
- ❌ **丢包 100%** → 继续以下步骤

| 步骤 | 操作 | 命令 |
|:--:|------|------|
| 2 | 编辑 netplan 配置 | `sudo nano /etc/netplan/50-cloud-init.yaml` |
| 3 | 修正权限 | `sudo chmod 600 /etc/netplan/*.yaml` |
| 4 | 生成并测试 | `sudo netplan generate && sudo netplan try` |
| 5 | 再次验证 | `ping 192.168.123.99` |
| 6 | 永久应用 | `sudo netplan apply` |

**步骤 2 配置内容：**

```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    enp6s0:
      dhcp4: no
      addresses:
        - 192.168.123.99/24
      optional: true
```

**步骤 4 说明：** `netplan try` 会临时应用配置，验证网络正常后按 **Enter** 保存；超时自动回滚。

---

## 三、部署与运行代码

**前提：** 服务器已 ping 通机器人，可执行文件已在本机交叉编译（`build-x86_64/`）。

### 上传

在本机用 `scp` 将可执行文件推送到服务器对应项目目录：

```bash
# 模板
scp <本地路径> ubuntu@<serverip>:~/server/beigongshang/<组名>/

# 示例：上传 mechdance 组的两个测试程序
scp ~/workspace/g1-mechdance/build-x86_64/arm_joint_test \
      ~/workspace/g1-mechdance/build-x86_64/leg_joint_test \
      ubuntu@10.101.194.227:~/server/beigongshang/mechdance/
```

### 运行

在服务器上进入对应目录，指定 DDS 库路径后执行：

```bash
# 模板
cd ~/server/beigongshang/<组名>
LD_LIBRARY_PATH=/usr/local/lib:. ./<可执行文件> enp6s0

# 示例
cd ~/server/beigongshang/mechdance
LD_LIBRARY_PATH=/usr/local/lib:. ./leg_joint_test enp6s0
```

| 参数 | 含义 |
|------|------|
| `LD_LIBRARY_PATH` | 追加 `/usr/local/lib`（DDS 库）和 `.`（当前目录） |
| `enp6s0` | 与机器人通信的有线网卡接口 |

---

## 快速参考

### 网络地址

| 目标 | IP | 接口 | 说明 |
|------|-----|------|------|
| 服务器（WiFi） | `10.101.x.x` | `wlx...` | 动态分配，登录后 `ip a` 确认 |
| 机器人（有线） | `192.168.123.99` | `enp6s0` | 固定 IP，netplan 配置保证 |

### 组名与路径

| 组名 | 服务器路径 |
|------|------|
| mechdance | `~/server/beigongshang/mechdance/` |
| helloword | `~/server/beigongshang/helloword/` |
| QKWL | `~/server/beigongshang/QKWL/` |

> QKWL = 乾坤武林，为避免中文字符兼容问题使用首字母。

### 常用命令速查

```bash
# SSH 到服务器
ssh ubuntu@<serverip>

# Ping 机器人
ping 192.168.123.99

# 修复机器人网络
sudo netplan generate && sudo netplan try && sudo netplan apply

# 上传可执行文件
scp <本地文件> ubuntu@<serverip>:~/server/beigongshang/<组名>/

# 运行程序
cd ~/server/beigongshang/<组名>
LD_LIBRARY_PATH=/usr/local/lib:. ./<可执行文件> enp6s0
```
